{
  "name": "@nextbus/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dataset:build": "node --import tsx scripts/build-dataset.mts",
    "dataset:publish": "node --import tsx scripts/publish-dataset.mts"
  },
  "dependencies": {
    "@nextbus/core": "workspace:*",
    "@nextbus/data-normalize": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260727.1",
    "@nextbus/contract": "workspace:*",
    "@nextbus/tsconfig": "workspace:*",
    "tsx": "^4.23.1",
    "typescript": "5.9.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.114.0"
  }
}
