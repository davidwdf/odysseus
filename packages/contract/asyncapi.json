{
  "asyncapi": "3.0.0",
  "x-json-schema-dialect": "https://json-schema.org/draft/2020-12/schema",
  "info": {
    "title": "NextBus HK — live ETA stream",
    "version": "1.0.0",
    "description": "The frames on `/v1/live`: one socket per client, a `snapshot` then `delta`s, pushed on a\nserver-controlled cadence. Companion to `openapi.json`, which describes the JSON endpoints.\n\nBoth documents are emitted from the same Zod declarations, through one registry pass, with one set\nof `#/components/schemas/` pointers — so they cannot disagree about what an `Eta` *is*. They are\n**not** byte-identical, and the two differences are deliberate losses of information that\nAsyncAPI's own rules force (see `foldRefSiblings` in `src/asyncapi.ts`): this document drops the\n`$id` every schema carries there, and it rewrites `$ref`-with-`description` into\n`allOf: [{$ref}, {description}]` at four sites — `Eta.destination`, `Eta.remark`,\n`Eta.remarkKind` and `RouteDetail.reverse` — because AsyncAPI's Reference Object ignores sibling\nkeywords, and `Eta.remarkKind`'s note (absent, never `\"info\"`) is the one field note in this\ncontract most expensive to lose. A generator therefore produces slightly different shapes for\nthose four from the two documents; everything else matches.\n\n**Read this before planning to generate anything from this document.** It is a specification\nartefact with a validator, and only aspirationally a codegen input:\n\n- **There is no AsyncAPI→Swift generator in existence.** Modelina — the reference generator —\n  emits 12 languages and Swift is not among them, and a sweep of `asyncapi.com/tools` finds no\n  tool mentioning Swift, Objective-C or iOS in any category. An iOS client hand-writes these\n  frame types.\n- **Kotlin generation exists and cannot serialise.** Modelina's Kotlin output lists JSON, XML\n  and binary serialization as \"currently not supported\", so `generate models kotlin` yields\n  annotation-free data classes and the decode layer is hand-written anyway — which puts the\n  unknown-enum tolerance obligation (`x-unknown-tolerant`) on hand-written code on Android too.\n- **`asyncapi diff` is not an `oasdiff` equivalent.** Its standards table has no\n  `/components/schemas/*` pointer, so adding *or removing* a payload field classifies as\n  `unclassified`, not `breaking`. A gate built on it would go green on a removed field — a gate\n  looking at nothing. Breaking-change discipline for these shapes is ADR-052 §5 plus review.\n- **The schemas are generated as JSON Schema 2020-12** and this document declares draft-07,\n  because that is what AsyncAPI 3.0's Schema Object is a superset of. The\n  `x-json-schema-dialect` extension above records what the bytes are, and\n  `packages/contract/scripts/check-asyncapi-current.mjs` fails on any keyword outside the\n  intersection of the two vocabularies, so the declaration stays true as the schemas grow.\n- **This document has never been validated against the AsyncAPI meta-schema.** Doing that needs\n  a dependency this repo has not taken (`@asyncapi/parser`, or `@asyncapi/specs` + ajv). The\n  gate transcribes the field lists and constraints that matter from the spec instead, and says\n  so. Treat \"AsyncAPI 3.0\" here as a careful reading, not a validator's verdict.\n\nConventions inherited from `openapi.json` and restated because a reader may only ever see this\nfile:\n\n- **Objects are open.** Additional properties are permitted by design; do not configure a\n  generator to reject unknown keys, or one added optional field stops every deployed client\n  decoding.\n- **Enums marked `x-unknown-tolerant` will gain members** without a major version bump.\n  Generate a fallback case; do not throw on an unrecognised value.\n- **A frame is identified by its `type` const, which is required on every frame.** That is what\n  makes the channel's \"valid against one and only one message\" rule hold given open objects.\n- **`Eta.dataTimestamp` carries a `+08:00` offset; a frame's own `at` and `Eta.observedAt` are\n  `Z`-suffixed UTC**, because our layer stamps those two and the operators stamp the other.\n  Parse them as instants. Judge staleness from `dataTimestamp` — never from `observedAt`, which\n  would make a replayed offline reading look fresh (ADR-008, ADR-058).\n- **ETAs are approximations.** No per-second countdown; repaint when a frame arrives."
  },
  "defaultContentType": "application/json",
  "servers": {
    "dev": {
      "host": "localhost:8787",
      "pathname": "/v1/live",
      "protocol": "ws",
      "description": "The local `wrangler dev` Worker (`pnpm dev:edge`). No production host is declared because none exists yet — see WP0-5. A deployed origin is `wss://<host>` with the same pathname."
    }
  },
  "channels": {
    "live": {
      "address": "/v1/live",
      "title": "Live ETA stream",
      "summary": "One socket per client. The client declares its targets in the connect URL and again in a `subscribe` frame; the Worker routes it to a shard, and the shard pushes a `snapshot` then `delta`s on an adaptive alarm cadence (~45–60 s, which is the measured upstream refresh interval — a faster cadence buys no newer data).",
      "messages": {
        "Subscribe": {
          "$ref": "#/components/messages/Subscribe"
        },
        "Ping": {
          "$ref": "#/components/messages/Ping"
        },
        "Snapshot": {
          "$ref": "#/components/messages/Snapshot"
        },
        "Delta": {
          "$ref": "#/components/messages/Delta"
        },
        "Status": {
          "$ref": "#/components/messages/Status"
        },
        "Pong": {
          "$ref": "#/components/messages/Pong"
        }
      },
      "bindings": {
        "ws": {
          "method": "GET",
          "query": {
            "type": "object",
            "properties": {
              "targets": {
                "type": "string",
                "description": "Comma-separated canonical stop or place ids. **Percent-encode the value** — place ids contain \"+\", which a query string decodes as a space, so an unencoded `P:KMB:a+KMB:b` arrives as a malformed id and is dropped; this is the same requirement `/v1/stop/{id}` states for the same reason. The **server** derives the shard from this list (sorted ids → FNV-1a over the lowest → `% shardCount`, `liveShardFor` in `@nextbus/core`), so a client with a stale shard count cannot silently land on a different shard — it does not compute one. The `subscribe` frame re-declares the same set, with optional per-stop route narrowing this parameter cannot express."
              }
            },
            "required": [
              "targets"
            ]
          },
          "bindingVersion": "0.1.0"
        }
      }
    }
  },
  "operations": {
    "receiveClientFrame": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/live"
      },
      "summary": "Frames the client sends to the Worker.",
      "messages": [
        {
          "$ref": "#/channels/live/messages/Subscribe"
        },
        {
          "$ref": "#/channels/live/messages/Ping"
        }
      ]
    },
    "sendServerFrame": {
      "action": "send",
      "channel": {
        "$ref": "#/channels/live"
      },
      "summary": "Frames the Worker pushes to the client.",
      "messages": [
        {
          "$ref": "#/channels/live/messages/Snapshot"
        },
        {
          "$ref": "#/channels/live/messages/Delta"
        },
        {
          "$ref": "#/channels/live/messages/Status"
        },
        {
          "$ref": "#/channels/live/messages/Pong"
        }
      ]
    }
  },
  "components": {
    "messages": {
      "Subscribe": {
        "name": "Subscribe",
        "summary": "Declare the complete target set for this connection.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/SubscribeFrame"
        }
      },
      "Ping": {
        "name": "Ping",
        "summary": "Keepalive. Auto-answered without waking the shard.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/PingFrame"
        }
      },
      "Snapshot": {
        "name": "Snapshot",
        "summary": "The accepted target set and every reading held for it.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/SnapshotFrame"
        }
      },
      "Delta": {
        "name": "Delta",
        "summary": "Readings that changed, and readings that are gone.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/DeltaFrame"
        }
      },
      "Status": {
        "name": "Status",
        "summary": "The connection state, and the failure behind it.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/StatusFrame"
        }
      },
      "Pong": {
        "name": "Pong",
        "summary": "The keepalive answer.",
        "contentType": "application/json",
        "payload": {
          "$ref": "#/components/schemas/PongFrame"
        }
      }
    },
    "schemas": {
      "Locale": {
        "type": "string",
        "enum": [
          "en",
          "zh-Hant",
          "zh-Hans"
        ],
        "x-unknown-tolerant": true
      },
      "I18nText": {
        "type": "object",
        "properties": {
          "en": {
            "type": "string"
          },
          "zh-Hant": {
            "type": "string"
          },
          "zh-Hans": {
            "type": "string"
          }
        },
        "required": [
          "en",
          "zh-Hant",
          "zh-Hans"
        ]
      },
      "OperatorId": {
        "type": "string",
        "enum": [
          "KMB",
          "LWB",
          "CTB",
          "GMB"
        ],
        "x-unknown-tolerant": true
      },
      "Bound": {
        "type": "string",
        "enum": [
          "inbound",
          "outbound"
        ],
        "x-unknown-tolerant": true
      },
      "LatLng": {
        "type": "object",
        "properties": {
          "lat": {
            "type": "number"
          },
          "lng": {
            "type": "number"
          }
        },
        "required": [
          "lat",
          "lng"
        ]
      },
      "Operator": {
        "type": "object",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/OperatorId"
          },
          "name": {
            "$ref": "#/components/schemas/I18nText"
          }
        },
        "required": [
          "id",
          "name"
        ]
      },
      "RemarkKind": {
        "type": "string",
        "enum": [
          "scheduled",
          "lastBus",
          "info"
        ],
        "x-unknown-tolerant": true
      },
      "Eta": {
        "type": "object",
        "properties": {
          "routeId": {
            "type": "string"
          },
          "stopId": {
            "type": "string"
          },
          "operator": {
            "$ref": "#/components/schemas/OperatorId"
          },
          "arrivals": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Up to ~3 upcoming arrivals, ISO-8601 with a +08:00 offset, soonest first."
          },
          "destination": {
            "allOf": [
              {
                "$ref": "#/components/schemas/I18nText"
              },
              {
                "description": "Where this service is headed, for flat ETA lists that show \"→ dest\" without the full Route object (e.g. Nearby). Server-populated; optional because not every feed/path supplies it."
              }
            ]
          },
          "fare": {
            "description": "Adult fare for boarding this route *at this stop*, HK$ decimal string. Sectional — see RouteServiceInfo. Compare numerically, display verbatim, never parse to float.",
            "type": "string"
          },
          "remark": {
            "allOf": [
              {
                "$ref": "#/components/schemas/I18nText"
              },
              {
                "description": "Free-text operator remark, if any (e.g. scheduled vs real-time). A \"Scheduled\" remark is a lower-confidence reading, not a live one — `remarkKind` is the classification to branch on; this is the wording to show."
              }
            ]
          },
          "remarkKind": {
            "allOf": [
              {
                "$ref": "#/components/schemas/RemarkKind"
              },
              {
                "description": "The coarse class of `remark`, classified server-side (ADR-053: the server owns content, the client owns the tone it renders in). Absent when there is no remark — and, for a client older than this field, derivable from `remark` with `classifyRemark` in `@nextbus/core`, which is the same function the server calls."
              }
            ]
          },
          "dataTimestamp": {
            "type": "string",
            "description": "When the upstream feed generated this reading. ISO-8601 with a +08:00 offset."
          },
          "observedAt": {
            "type": "string",
            "description": "When our layer fetched/observed it, and the one field here we stamp ourselves. **ISO-8601 `Z`-suffixed UTC, not the `+08:00` the conventions list describes** — the normalizers use `Date#toISOString()`. Parse it as an instant; never compare it lexically against `dataTimestamp`, which carries the upstream offset. Survives offline replay (ADR-058) so a client can tell a replayed reading from a fetched one — but **judge staleness from `dataTimestamp`**, which is the operator clock the ETA helpers age a reading by."
          }
        },
        "required": [
          "routeId",
          "stopId",
          "operator",
          "arrivals",
          "dataTimestamp",
          "observedAt"
        ]
      },
      "ServiceDayType": {
        "type": "string",
        "enum": [
          "weekday",
          "saturday",
          "sunday",
          "daily",
          "other"
        ],
        "x-unknown-tolerant": true
      },
      "FreqBand": {
        "type": "object",
        "properties": {
          "start": {
            "type": "string",
            "description": "Local 24h \"HH:mm\". May exceed 24 — \"25:35\" means 01:35 next day."
          },
          "end": {
            "type": "string",
            "description": "Local 24h \"HH:mm\". May exceed 24 — \"25:35\" means 01:35 next day."
          },
          "headwayMin": {
            "type": "number"
          }
        },
        "required": [
          "start",
          "end",
          "headwayMin"
        ]
      },
      "FreqPattern": {
        "type": "object",
        "properties": {
          "dayType": {
            "$ref": "#/components/schemas/ServiceDayType"
          },
          "days": {
            "type": "array",
            "items": {
              "type": "boolean"
            }
          },
          "bands": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FreqBand"
            }
          },
          "first": {
            "type": "string",
            "description": "Earliest first departure across the bands, \"HH:mm\" (may exceed 24)."
          },
          "last": {
            "type": "string",
            "description": "Latest last departure across the bands, \"HH:mm\" (may exceed 24)."
          }
        },
        "required": [
          "dayType",
          "days",
          "bands",
          "first",
          "last"
        ]
      },
      "RouteServiceSummary": {
        "type": "object",
        "properties": {
          "fareFull": {
            "description": "Full adult fare from the route origin, HK$ as a decimal string, e.g. \"6.7\". Compare numerically, display verbatim, never parse to float.",
            "type": "string"
          },
          "fareFullHoliday": {
            "description": "Holiday full fare, HK$ decimal string. Present only when it differs from fareFull.",
            "type": "string"
          },
          "journeyMin": {
            "description": "Whole-route journey time, minutes.",
            "type": "number"
          },
          "headway": {
            "description": "Typical headway from the GTFS frequency bands, minutes. Coarse range — no fake precision.",
            "type": "object",
            "properties": {
              "min": {
                "type": "number"
              },
              "max": {
                "type": "number"
              }
            },
            "required": [
              "min",
              "max"
            ]
          },
          "hours": {
            "description": "Rough daily service span, local 24h \"HH:mm\" (may exceed 24). Earliest first departure → latest end.",
            "type": "object",
            "properties": {
              "start": {
                "type": "string"
              },
              "end": {
                "type": "string"
              }
            },
            "required": [
              "start",
              "end"
            ]
          }
        },
        "description": "Static service facts at the **summary** tier — no frequency profiles. Served by /v1/stop/{id}. There is deliberately no `patterns` property here: the profiles are large and are read on one screen only, so duplicating them into every place a route touches is not worth 54 MB of the build. Load /v1/route/{id} for them."
      },
      "RouteServiceInfo": {
        "type": "object",
        "properties": {
          "fareFull": {
            "description": "Full adult fare from the route origin, HK$ as a decimal string, e.g. \"6.7\". Compare numerically, display verbatim, never parse to float.",
            "type": "string"
          },
          "fareFullHoliday": {
            "description": "Holiday full fare, HK$ decimal string. Present only when it differs from fareFull.",
            "type": "string"
          },
          "journeyMin": {
            "description": "Whole-route journey time, minutes.",
            "type": "number"
          },
          "headway": {
            "description": "Typical headway from the GTFS frequency bands, minutes. Coarse range — no fake precision.",
            "type": "object",
            "properties": {
              "min": {
                "type": "number"
              },
              "max": {
                "type": "number"
              }
            },
            "required": [
              "min",
              "max"
            ]
          },
          "hours": {
            "description": "Rough daily service span, local 24h \"HH:mm\" (may exceed 24). Earliest first departure → latest end.",
            "type": "object",
            "properties": {
              "start": {
                "type": "string"
              },
              "end": {
                "type": "string"
              }
            },
            "required": [
              "start",
              "end"
            ]
          },
          "patterns": {
            "description": "Per-day-type frequency profiles (ADR-044). Absent here means the dataset has no frequency table for this route. A stop response cannot carry this field at all — it returns RouteServiceSummary.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FreqPattern"
            }
          }
        },
        "description": "Static service facts at the **full** tier — the summary fields plus `patterns`. Served by /v1/route/{id} only. An absent `patterns` here is a fact about the route (the dataset has no frequency table for it), not an artefact of which endpoint was called."
      },
      "RouteSummary": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Canonical route id, e.g. \"KMB:6:outbound:1\". GMB is \"GMB:{no}:{bound}:{gtfsId}\"."
          },
          "operator": {
            "$ref": "#/components/schemas/OperatorId"
          },
          "routeNo": {
            "type": "string",
            "description": "Public route number shown on the bus, e.g. \"6\", \"720\", \"N691\"."
          },
          "bound": {
            "$ref": "#/components/schemas/Bound"
          },
          "serviceType": {
            "type": "string",
            "description": "Operator service-type discriminator (KMB has variants per route). A string — some upstream entries are numeric and are coerced."
          },
          "origin": {
            "$ref": "#/components/schemas/I18nText"
          },
          "destination": {
            "$ref": "#/components/schemas/I18nText"
          },
          "service": {
            "$ref": "#/components/schemas/RouteServiceSummary"
          }
        },
        "required": [
          "id",
          "operator",
          "routeNo",
          "bound",
          "serviceType",
          "origin",
          "destination"
        ],
        "description": "A route as it appears in a stop response: identical to `Route` except that `service` is the summary tier (`RouteServiceSummary`) and therefore carries no frequency profiles."
      },
      "Route": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Canonical route id, e.g. \"KMB:6:outbound:1\". GMB is \"GMB:{no}:{bound}:{gtfsId}\"."
          },
          "operator": {
            "$ref": "#/components/schemas/OperatorId"
          },
          "routeNo": {
            "type": "string",
            "description": "Public route number shown on the bus, e.g. \"6\", \"720\", \"N691\"."
          },
          "bound": {
            "$ref": "#/components/schemas/Bound"
          },
          "serviceType": {
            "type": "string",
            "description": "Operator service-type discriminator (KMB has variants per route). A string — some upstream entries are numeric and are coerced."
          },
          "origin": {
            "$ref": "#/components/schemas/I18nText"
          },
          "destination": {
            "$ref": "#/components/schemas/I18nText"
          },
          "service": {
            "$ref": "#/components/schemas/RouteServiceInfo"
          }
        },
        "required": [
          "id",
          "operator",
          "routeNo",
          "bound",
          "serviceType",
          "origin",
          "destination"
        ],
        "description": "A route at full service fidelity (`service` is `RouteServiceInfo`, including `patterns`). Returned by /v1/route/{id}; stop responses return `RouteSummary` instead."
      },
      "RouteStop": {
        "type": "object",
        "properties": {
          "routeId": {
            "type": "string"
          },
          "seq": {
            "type": "number",
            "description": "1-based position along the route."
          },
          "stopId": {
            "type": "string"
          }
        },
        "required": [
          "routeId",
          "seq",
          "stopId"
        ]
      },
      "RouteRef": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Canonical route id, e.g. \"KMB:6:inbound:1\"."
          },
          "origin": {
            "$ref": "#/components/schemas/I18nText"
          },
          "destination": {
            "$ref": "#/components/schemas/I18nText"
          }
        },
        "required": [
          "id",
          "origin",
          "destination"
        ]
      },
      "Stop": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "$ref": "#/components/schemas/I18nText"
          },
          "location": {
            "$ref": "#/components/schemas/LatLng"
          },
          "sources": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "operator": {
                  "$ref": "#/components/schemas/OperatorId"
                },
                "operatorStopId": {
                  "type": "string"
                }
              },
              "required": [
                "operator",
                "operatorStopId"
              ]
            }
          },
          "bearingDeg": {
            "type": "number"
          }
        },
        "required": [
          "id",
          "name",
          "location",
          "sources"
        ]
      },
      "Place": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "$ref": "#/components/schemas/I18nText"
          },
          "location": {
            "$ref": "#/components/schemas/LatLng"
          },
          "stopIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "id",
          "name",
          "location",
          "stopIds"
        ]
      },
      "RouteDetail": {
        "type": "object",
        "properties": {
          "route": {
            "$ref": "#/components/schemas/Route"
          },
          "stops": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "seq": {
                  "type": "number"
                },
                "stop": {
                  "$ref": "#/components/schemas/Stop"
                },
                "eta": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Eta"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "fare": {
                  "description": "Boarding fare at this stop, HK$ decimal string.",
                  "type": "string"
                }
              },
              "required": [
                "seq",
                "stop",
                "eta"
              ]
            }
          },
          "reverse": {
            "allOf": [
              {
                "$ref": "#/components/schemas/RouteRef"
              },
              {
                "description": "The same route number in the opposite direction, when the dataset carries one — lets the UI offer a direction toggle. Absent for circular / single-direction routes (ADR-046). Server-resolved with the correct service-type variant, so the client never guesses the id."
              }
            ]
          }
        },
        "required": [
          "route",
          "stops"
        ]
      },
      "StopDetail": {
        "type": "object",
        "properties": {
          "stop": {
            "$ref": "#/components/schemas/Stop"
          },
          "routes": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "route": {
                  "$ref": "#/components/schemas/RouteSummary"
                },
                "eta": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Eta"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "fare": {
                  "description": "Boarding fare here, HK$ decimal string.",
                  "type": "string"
                },
                "stopId": {
                  "type": "string",
                  "description": "Canonical id of the member pole this route departs from."
                }
              },
              "required": [
                "route",
                "eta",
                "stopId"
              ]
            }
          },
          "members": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "name": {
                  "$ref": "#/components/schemas/I18nText"
                },
                "location": {
                  "$ref": "#/components/schemas/LatLng"
                }
              },
              "required": [
                "id",
                "name",
                "location"
              ]
            }
          }
        },
        "required": [
          "stop",
          "routes",
          "members"
        ]
      },
      "NearbyStop": {
        "type": "object",
        "properties": {
          "stop": {
            "$ref": "#/components/schemas/Stop"
          },
          "distanceM": {
            "type": "number",
            "description": "Straight-line distance from the query point, metres. Round it for display — ADR-008."
          },
          "etas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Eta"
            },
            "description": "Soonest arrivals, deduped to one per route+direction. May be fewer than routeCount — routes with no live reading right now are not listed."
          },
          "routeCount": {
            "type": "number",
            "description": "True number of distinct routes serving the place, from the static index (no live call). Lets a compact card say \"soonest few of N · +N more\" honestly, never a silent filter."
          }
        },
        "required": [
          "stop",
          "distanceM",
          "etas",
          "routeCount"
        ]
      },
      "ClientPolicy": {
        "type": "object",
        "properties": {
          "dueUnderSec": {
            "description": "Under this many seconds to arrival, show \"Due\"/\"Arriving\" rather than a number (ADR-008 — never fabricate sub-minute precision). Also the symmetric tolerance band for treating a just-passed reading as still present rather than departed.",
            "type": "number"
          },
          "warnUnderSec": {
            "description": "Under this many seconds, the arrival is imminent enough to deserve emphasis. The threshold is the server's; **how** it is emphasised is the client's — a tone, a weight, a haptic. See ADR-053.",
            "type": "number"
          },
          "staleAfterMs": {
            "description": "A reading whose `dataTimestamp` is older than this is presented as stale. Data quality, not an error: the value is still shown, labelled.",
            "type": "number"
          },
          "refreshAfterMs": {
            "description": "How often a client should refetch live arrivals. Matched to the edge's own coalescing TTL (ADR-057) — polling faster returns a byte-identical cached response and buys nothing but battery.",
            "type": "number"
          },
          "maxArrivals": {
            "description": "How many upcoming arrivals one route row shows.",
            "type": "number"
          },
          "maxRows": {
            "description": "How many route rows a compact stop card shows before it collapses to \"+N more\".",
            "type": "number"
          }
        }
      },
      "RouteLite": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "operator": {
            "$ref": "#/components/schemas/OperatorId"
          },
          "routeNo": {
            "type": "string"
          },
          "bound": {
            "$ref": "#/components/schemas/Bound"
          },
          "origin": {
            "$ref": "#/components/schemas/I18nText"
          },
          "destination": {
            "$ref": "#/components/schemas/I18nText"
          },
          "sortKey": {
            "description": "Byte-comparable ordering key for `routeNo`, zero-padded per numeric run: `10A` → `0010A`, so `9` < `10A` < `11`. Sort by this and every platform agrees; `localeCompare(numeric:true)` has no faithful Swift/Kotlin equivalent (ADR-063). Optional per ADR-052 §5: a client that receives none derives the identical value with `routeSortKey` in `@nextbus/core`.",
            "type": "string"
          }
        },
        "required": [
          "id",
          "operator",
          "routeNo",
          "bound",
          "origin",
          "destination"
        ]
      },
      "StopLite": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "$ref": "#/components/schemas/I18nText"
          },
          "lat": {
            "type": "number"
          },
          "lng": {
            "type": "number"
          }
        },
        "required": [
          "id",
          "name",
          "lat",
          "lng"
        ]
      },
      "SearchIndex": {
        "type": "object",
        "properties": {
          "version": {
            "type": "string",
            "description": "Content hash of `routes` + `stops` (SHA-256, first 16 hex). Moves exactly when the index moves, and is also served as the endpoint's strong ETag, so a returning client revalidates with `If-None-Match` and pays a 304 (ADR-063)."
          },
          "routes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RouteLite"
            }
          },
          "stops": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StopLite"
            }
          }
        },
        "required": [
          "version",
          "routes",
          "stops"
        ]
      },
      "ErrorCode": {
        "type": "string",
        "enum": [
          "bad_request",
          "not_found",
          "internal",
          "upstream_unavailable",
          "upstream_timeout"
        ],
        "x-unknown-tolerant": true
      },
      "WireError": {
        "type": "object",
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode"
          },
          "message": {
            "type": "string",
            "description": "Human-readable, English, and **not** a stable identifier — it names the offending value and its wording changes freely. Do not match on it."
          },
          "retryable": {
            "type": "boolean",
            "description": "Whether the identical request may succeed later. `false` means the request is permanently wrong (a malformed or deleted id) and a background client — an iOS Widget, a watch complication — should prune it rather than retry."
          }
        },
        "required": [
          "code",
          "message",
          "retryable"
        ]
      },
      "ErrorResponse": {
        "type": "object",
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode"
          },
          "message": {
            "type": "string",
            "description": "Human-readable, English, and **not** a stable identifier — it names the offending value and its wording changes freely. Do not match on it."
          },
          "retryable": {
            "type": "boolean",
            "description": "Whether the identical request may succeed later. `false` means the request is permanently wrong (a malformed or deleted id) and a background client — an iOS Widget, a watch complication — should prune it rather than retry."
          },
          "error": {
            "type": "string",
            "deprecated": true,
            "description": "Duplicate of `message`, kept for pre-ADR-064 clients. Branch on `code`."
          }
        },
        "required": [
          "code",
          "message",
          "retryable",
          "error"
        ]
      },
      "DatasetOrigin": {
        "type": "string",
        "enum": [
          "kv",
          "inline"
        ],
        "x-unknown-tolerant": true
      },
      "HealthResponse": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          },
          "dataset": {
            "$ref": "#/components/schemas/DatasetOrigin"
          },
          "buildHash": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "datasetBuildsThisIsolate": {
            "type": "number"
          }
        },
        "required": [
          "ok",
          "dataset",
          "buildHash",
          "datasetBuildsThisIsolate"
        ]
      },
      "RootResponse": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "ok"
        ]
      },
      "EtaList": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/Eta"
        }
      },
      "NearbyList": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/NearbyStop"
        }
      },
      "WatchTarget": {
        "type": "object",
        "properties": {
          "stopId": {
            "type": "string",
            "description": "Canonical stop id (\"KMB:1234\") or merged place id (\"P:<a>+<b>\"). Validated with the same parser the HTTP endpoints use, so a malformed favourite is dropped identically on every transport."
          },
          "routeIds": {
            "description": "Canonical route ids to narrow the stop to. **Absent means every route at the stop**; an empty array is not a legal narrowing (it asks for nothing) and is rejected rather than honoured.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "stopId"
        ]
      },
      "EtaRef": {
        "type": "object",
        "properties": {
          "stopId": {
            "type": "string"
          },
          "routeId": {
            "type": "string"
          }
        },
        "required": [
          "stopId",
          "routeId"
        ]
      },
      "LiveState": {
        "type": "string",
        "enum": [
          "connecting",
          "live",
          "retrying",
          "closed"
        ],
        "x-unknown-tolerant": true
      },
      "SubscribeFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "subscribe"
          },
          "targets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WatchTarget"
            },
            "description": "The complete target set for this connection, replacing whatever it declared before. An empty array is a legal frame — it means \"stop sending me readings\" without closing the socket."
          }
        },
        "required": [
          "type",
          "targets"
        ]
      },
      "PingFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "ping"
          }
        },
        "required": [
          "type"
        ]
      },
      "PongFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "pong"
          }
        },
        "required": [
          "type"
        ]
      },
      "SnapshotFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "snapshot"
          },
          "seq": {
            "type": "integer",
            "minimum": -9007199254740991,
            "maximum": 9007199254740991,
            "description": "Monotonic frame counter for this connection, starting at 1. A client compares it with the last frame it applied to notice a gap; `0` is never sent — it is the client-side \"nothing applied yet\" sentinel."
          },
          "at": {
            "type": "string",
            "description": "When the server assembled this frame. ISO-8601, **`Z`-suffixed UTC** — not the `+08:00` the conventions list describes, because this is stamped by our own layer with `Date#toISOString()` exactly as `Eta.observedAt` is. Parse it as an instant; never compare it lexically against a `+08:00` timestamp such as `Eta.dataTimestamp`."
          },
          "targets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WatchTarget"
            },
            "description": "The target set the server **accepted** — not the set that was asked for. Compare it with what you sent and tell the rider about the difference."
          },
          "etas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Eta"
            },
            "description": "Every reading the server currently holds for the accepted targets, in canonical (stopId, routeId) code-point order. A route with no reading right now is simply absent, exactly as in `/v1/etas/{id}`."
          }
        },
        "required": [
          "type",
          "seq",
          "at",
          "targets",
          "etas"
        ]
      },
      "DeltaFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "delta"
          },
          "seq": {
            "type": "integer",
            "minimum": -9007199254740991,
            "maximum": 9007199254740991,
            "description": "Monotonic frame counter for this connection. A receiver expecting `seq` and given `seq + 2` has missed a frame: apply this one, then ask for a fresh snapshot."
          },
          "at": {
            "type": "string",
            "description": "When the server assembled this frame. ISO-8601 `Z`-suffixed UTC — see SnapshotFrame."
          },
          "changed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Eta"
            },
            "description": "Readings that are new or whose content differs from the last one sent, in canonical (stopId, routeId) order. \"Differs\" excludes `observedAt`: a re-observation that yields the identical operator reading is not a change, and reporting it would make every round a full repaint."
          },
          "gone": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EtaRef"
            },
            "description": "Readings the server no longer has — the bus departed, or the target was dropped. Remove them; do not leave the last value on screen."
          }
        },
        "required": [
          "type",
          "seq",
          "at",
          "changed",
          "gone"
        ]
      },
      "StatusFrame": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "const": "status"
          },
          "at": {
            "type": "string",
            "description": "When the server assembled this frame. ISO-8601 `Z`-suffixed UTC — see SnapshotFrame."
          },
          "state": {
            "$ref": "#/components/schemas/LiveState"
          },
          "error": {
            "allOf": [
              {
                "$ref": "#/components/schemas/WireError"
              },
              {
                "description": "Present when `state` reflects a failure. Branch on `code`; let `retryable` decide whether to try again or to stop asking. Absent on an ordinary transition."
              }
            ]
          }
        },
        "required": [
          "type",
          "at",
          "state"
        ]
      },
      "ClientFrame": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/SubscribeFrame"
          },
          {
            "$ref": "#/components/schemas/PingFrame"
          }
        ]
      },
      "ServerFrame": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/SnapshotFrame"
          },
          {
            "$ref": "#/components/schemas/DeltaFrame"
          },
          {
            "$ref": "#/components/schemas/StatusFrame"
          },
          {
            "$ref": "#/components/schemas/PongFrame"
          }
        ]
      }
    }
  }
}
