{
  "$description": "The one declaration of every design value in NextBus HK (docs/09-theme.md). DTCG format (Design Tokens Community Group): groups are plain nesting, leaves carry $value/$type/$description, and {dotted.path} is an alias. `pnpm --filter @nextbus/ui tokens:emit` derives every consumer from this file — the Tailwind/NativeWind preset, the web CSS variables, the TypeScript module, and the Swift/Kotlin constants a native client reads. Nothing here is repeated anywhere else; if you find a colour or a radius written down twice, one of them is a bug. Two deliberate deviations from the DTCG draft, both so the output stays lossless: (1) `fontWeight` $values are our four Inter cut names, not the spec's canonical aliases, because a cut name is also the RN `fontFamily` value; (2) shadow alpha and Android's Material dp live in `$extensions.com.nextbus` rather than being folded into an 8-bit hex alpha, which would round 0.06 to 0.059.",
  "palette": {
    "$type": "color",
    "$description": "Primitive tokens — the raw palette (docs/09 §1, layer 1). Components never reference these; the semantic tokens below alias them. A value that appears here once and is aliased twice is the point: #111827 is the brand ink, the light-mode text colour, the light-mode accent and the light-mode focus ring, and it is written down once.",
    "white": { "$value": "#FFFFFF" },
    "black": { "$value": "#000000" },
    "slate-50": { "$value": "#F8FAFC" },
    "slate-100": { "$value": "#F1F5F9" },
    "slate-200": { "$value": "#E2E8F0" },
    "slate-500": { "$value": "#64748B" },
    "slate-600": { "$value": "#475569" },
    "slate-900": {
      "$value": "#0F172A",
      "$description": "The ink every cast shadow is drawn in, and the dark text that sits on the yellow CTB accent. Deliberately not a semantic theme token: shadows do not re-skin with the appearance."
    },
    "ink": { "$value": "#111827" },
    "ink-950": { "$value": "#0D111C" },
    "ink-900": { "$value": "#161B29" },
    "ink-800": { "$value": "#202636" },
    "ink-700": { "$value": "#2C3343" },
    "paper": { "$value": "#F4F6FA" },
    "grey-400": { "$value": "#9EA5B4" },
    "grey-500": { "$value": "#6B7280" },
    "green-600": { "$value": "#16A34A" },
    "green-500": { "$value": "#22C55E" },
    "amber-600": { "$value": "#D97706" },
    "amber-500": { "$value": "#F59E0B" },
    "red-600": { "$value": "#DC2626" },
    "red-500": { "$value": "#EF4444" },
    "rose-600": { "$value": "#E11D48" },
    "kmb-red": { "$value": "#D7282F" },
    "lwb-gold": { "$value": "#E8A33D" },
    "ctb-yellow": { "$value": "#F6C700" },
    "gmb-green": { "$value": "#00845C" }
  },
  "color": {
    "$type": "color",
    "semantic": {
      "$description": "Semantic tokens — the only colours a component may name (docs/09 §1–2). One Ink theme in two modes (ADR-029 retired the livery axis): the accent is the *ink* on light (a dark mark on a white page) and inverts to *paper* on dark. Every token declares both modes, and the generator fails if one mode is missing a token the other has. `cssVar` is the variable the value is published as; `tailwind` is the utility name it is reachable by. The emitted CSS holds \"R G B\" triplets rather than hex so `rgb(var(--x) / <alpha-value>)` keeps Tailwind's alpha modifiers working.",
      "bg": {
        "$description": "Page background.",
        "$extensions": { "com.nextbus": { "cssVar": "--bg", "tailwind": "bg" } },
        "light": { "$value": "{palette.white}" },
        "dark": { "$value": "{palette.ink-950}" }
      },
      "surface": {
        "$description": "A raised surface — cards, sheets, rows.",
        "$extensions": { "com.nextbus": { "cssVar": "--surface", "tailwind": "surface" } },
        "light": { "$value": "{palette.slate-50}" },
        "dark": { "$value": "{palette.ink-900}" }
      },
      "surface-2": {
        "$description": "The second lift. On dark this carries the elevation cue that a shadow carries on light (ADR-035).",
        "$extensions": { "com.nextbus": { "cssVar": "--surface-2", "tailwind": "surface-2" } },
        "light": { "$value": "{palette.slate-100}" },
        "dark": { "$value": "{palette.ink-800}" }
      },
      "border": {
        "$description": "Hairline divider and the silhouette a dark-mode surface uses instead of a shadow.",
        "$extensions": { "com.nextbus": { "cssVar": "--border", "tailwind": "border" } },
        "light": { "$value": "{palette.slate-200}" },
        "dark": { "$value": "{palette.ink-700}" }
      },
      "text": {
        "$description": "Body text. ≥ 4.5:1 against `bg` in both modes.",
        "$extensions": { "com.nextbus": { "cssVar": "--text", "tailwind": "text" } },
        "light": { "$value": "{palette.ink}" },
        "dark": { "$value": "{palette.paper}" }
      },
      "text-muted": {
        "$description": "Secondary text. Also the tab bar's inactive tint — `text-subtle` was too low-contrast to read on glass (docs/09 §Glass legibility, rule 2).",
        "$extensions": { "com.nextbus": { "cssVar": "--text-muted", "tailwind": "muted" } },
        "light": { "$value": "{palette.slate-600}" },
        "dark": { "$value": "{palette.grey-400}" }
      },
      "text-subtle": {
        "$description": "Tertiary text — timestamps, captions. Never essential information.",
        "$extensions": { "com.nextbus": { "cssVar": "--text-subtle", "tailwind": "subtle" } },
        "light": { "$value": "{palette.slate-500}" },
        "dark": { "$value": "{palette.grey-500}" }
      },
      "accent": {
        "$description": "The monochrome accent — ink on light, paper on dark. Not a colour: operator identity and ETA status carry meaning, so the accent must not compete with them.",
        "$extensions": { "com.nextbus": { "cssVar": "--accent", "tailwind": "accent" } },
        "light": { "$value": "{palette.ink}" },
        "dark": { "$value": "{palette.slate-200}" }
      },
      "accent-contrast": {
        "$description": "What sits on top of `accent`.",
        "$extensions": {
          "com.nextbus": { "cssVar": "--accent-contrast", "tailwind": "accent-contrast" }
        },
        "light": { "$value": "{palette.white}" },
        "dark": { "$value": "{palette.ink-950}" }
      },
      "focus": {
        "$description": "Keyboard/web focus ring. Must stay visible in both modes (docs/09 §8).",
        "$extensions": { "com.nextbus": { "cssVar": "--focus", "tailwind": "focus" } },
        "light": { "$value": "{palette.ink}" },
        "dark": { "$value": "{palette.slate-200}" }
      },
      "positive": {
        "$description": "Arriving / good. Always paired with an icon or label — never colour alone.",
        "$extensions": { "com.nextbus": { "cssVar": "--positive", "tailwind": "positive" } },
        "light": { "$value": "{palette.green-600}" },
        "dark": { "$value": "{palette.green-500}" }
      },
      "warning": {
        "$description": "A few minutes away / uncertain.",
        "$extensions": { "com.nextbus": { "cssVar": "--warning", "tailwind": "warning" } },
        "light": { "$value": "{palette.amber-600}" },
        "dark": { "$value": "{palette.amber-500}" }
      },
      "danger": {
        "$description": "No service / error.",
        "$extensions": { "com.nextbus": { "cssVar": "--danger", "tailwind": "danger" } },
        "light": { "$value": "{palette.red-600}" },
        "dark": { "$value": "{palette.red-500}" }
      }
    },
    "brand": {
      "$description": "Fixed, theme-independent brand values. Not semantic tokens — they do not invert with the appearance.",
      "ink": {
        "$value": "{palette.ink}",
        "$description": "The app-icon field colour (apps/mobile/assets/icon.svg), the PWA `theme-color`, and a fixed dark glass tint (`bg-ink/55`). Promoted to a token so the icon, the splash, the browser chrome and any brand chrome stay one family."
      }
    },
    "operator": {
      "$description": "Operator brand accents — used sparingly (a route-number chip, a thin route line), never as a background. Unaffected by the appearance so operator identity is constant (docs/09 §7).",
      "KMB": { "$value": "{palette.kmb-red}" },
      "LWB": { "$value": "{palette.lwb-gold}" },
      "CTB": { "$value": "{palette.ctb-yellow}" },
      "GMB": { "$value": "{palette.gmb-green}" }
    },
    "operatorText": {
      "$description": "The contrast-safe text colour to sit on each operator accent. The yellow CTB accent always pairs with dark text, never white (docs/09 §2).",
      "KMB": { "$value": "{palette.white}" },
      "LWB": { "$value": "{palette.white}" },
      "CTB": { "$value": "{palette.slate-900}" },
      "GMB": { "$value": "{palette.white}" }
    },
    "map": {
      "$description": "Basemap overlay colours. The map is not themed (the LandsD tiles are inverted by filter instead — ADR-041/049), so these are fixed values chosen to read over both the light and the inverted-dark tiles.",
      "pin": {
        "$value": "{palette.rose-600}",
        "$description": "Last-resort pin fill for a stop with no known operator. A lone stop is coloured by its operator and each pole of a multi-pole place by its own, so this rose is only the fallback."
      },
      "pinBorder": {
        "$value": "{palette.white}",
        "$description": "The ring that separates a pin from the tiles underneath it."
      }
    }
  },
  "radius": {
    "$type": "dimension",
    "$description": "Corner radii (docs/09 §4). Cards `md`/`lg`; bottom sheets `sheet`; chips and pills `full`.",
    "sm": { "$value": { "value": 6, "unit": "px" } },
    "md": { "$value": { "value": 10, "unit": "px" } },
    "lg": { "$value": { "value": 14, "unit": "px" } },
    "xl": { "$value": { "value": 20, "unit": "px" } },
    "full": { "$value": { "value": 9999, "unit": "px" } },
    "pill": {
      "$value": { "value": 24, "unit": "px" },
      "$description": "The floating tab bar's rounded-pill corner (ADR-027). Off the sm–xl scale on purpose: the bar is a pill, and its radius tracks its 54px height, not the card scale."
    },
    "sheet": {
      "$value": { "value": 26, "unit": "px" },
      "$description": "The bottom sheet's top corners. Slightly softer than `xl` because the sheet spans the full width, where 20px reads tight."
    }
  },
  "spacing": {
    "$type": "dimension",
    "$description": "The 4px spacing scale (docs/09 §4). These are the same values Tailwind ships as its default rem-based scale — declared here so the native platforms, which have no Tailwind, read them from the same place. The emitted preset publishes them in rem so the web build keeps honouring the browser's font size. Touch targets stay ≥ 44×44px and adjacent tappables ≥ 8px apart, which is a rule, not a token.",
    "1": { "$value": { "value": 4, "unit": "px" } },
    "2": { "$value": { "value": 8, "unit": "px" } },
    "3": { "$value": { "value": 12, "unit": "px" } },
    "4": { "$value": { "value": 16, "unit": "px" } },
    "5": { "$value": { "value": 20, "unit": "px" } },
    "6": { "$value": { "value": 24, "unit": "px" } },
    "8": { "$value": { "value": 32, "unit": "px" } },
    "10": { "$value": { "value": 40, "unit": "px" } },
    "12": { "$value": { "value": 48, "unit": "px" } }
  },
  "motion": {
    "$type": "duration",
    "$description": "Animation durations (docs/09 §5). Micro-interactions stay in the 150–300ms band; all of them collapse to an instant swap under reduced motion.",
    "fast": { "$value": { "value": 120, "unit": "ms" } },
    "base": { "$value": { "value": 200, "unit": "ms" } },
    "slow": { "$value": { "value": 320, "unit": "ms" } }
  },
  "opacity": {
    "$type": "number",
    "etaStale": {
      "$value": 0.45,
      "$description": "Applied to an ETA reading that has gone stale. Honesty, not decoration: a dimmed number says \"this is old\" without inventing a fresher one (ADR-008)."
    }
  },
  "font": {
    "cut": {
      "$type": "fontFamily",
      "$description": "Inter is loaded as discrete weight cuts (apps/mobile/app/_layout.tsx). RN's `fontFamily` is single-valued, so a weight maps to its exact registered family name rather than to a numeric weight; CJK glyphs fall back to the OS face.",
      "regular": { "$value": "Inter_400Regular" },
      "medium": { "$value": "Inter_500Medium" },
      "semibold": { "$value": "Inter_600SemiBold" },
      "bold": { "$value": "Inter_700Bold" }
    },
    "fallback": {
      "$type": "fontFamily",
      "$value": ["Noto Sans HK", "Noto Sans SC", "PingFang HK", "system-ui", "sans-serif"],
      "$description": "The stack appended after every cut on web, so CJK and the no-webfont case land on a sensible face. Declared once: the generator composes each cut with it, which is why the tail is not written out four times in the preset. On native `fontFamily` is single-valued and the OS handles CJK fallback, so only the cut is used (ADR-019 — v1 bundles no CJK webfont)."
    }
  },
  "type": {
    "$description": "The named type scale (docs/09 §3), mobile-first on a 16px base. Components reference a role, never a raw size, so type stays consistent across every screen; `<Text variant>` is the canonical consumer.",
    "display": {
      "$description": "The hero ETA numeral.",
      "fontSize": { "$type": "dimension", "$value": { "value": 40, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 44, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "bold" }
    },
    "h1": {
      "$description": "Screen titles.",
      "fontSize": { "$type": "dimension", "$value": { "value": 28, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 34, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "bold" }
    },
    "h2": {
      "$description": "Section headers.",
      "fontSize": { "$type": "dimension", "$value": { "value": 22, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 28, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "semibold" }
    },
    "h3": {
      "$description": "Card titles and route numbers.",
      "fontSize": { "$type": "dimension", "$value": { "value": 18, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 24, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "semibold" }
    },
    "body": {
      "$description": "The default, and the minimum size on mobile.",
      "fontSize": { "$type": "dimension", "$value": { "value": 16, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 24, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "regular" }
    },
    "label": {
      "$description": "Secondary labels.",
      "fontSize": { "$type": "dimension", "$value": { "value": 14, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 20, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "medium" }
    },
    "caption": {
      "$description": "Timestamps only — never essential information.",
      "fontSize": { "$type": "dimension", "$value": { "value": 12, "unit": "px" } },
      "lineHeight": { "$type": "dimension", "$value": { "value": 16, "unit": "px" } },
      "weight": { "$type": "fontWeight", "$value": "regular" }
    }
  },
  "elevation": {
    "$type": "shadow",
    "$description": "Elevation recipes (docs/09 §4): `e0` none · `e1` cards · `e2` sticky headers · `e3` sheet/FAB/floating tab bar. Platform-neutral at source — a shadow geometry plus, where the platform wants one instead, Android's Material dp. `blur` carries CSS semantics (a 2σ blur radius), because that is what the DTCG shadow type means; the iOS recipe halves it to get `shadowRadius`, which is σ. Consumers never read a level directly: `elevationStyle(level, Platform.OS)` picks the recipe. On dark, prefer `surface-2` + `border` over any of these — a drop shadow has almost no contrast budget on a near-black field (ADR-035).",
    "e0": {
      "$description": "Flat on the surface. Every platform recipe collapses to an empty style.",
      "$value": {
        "color": "{palette.black}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 0, "unit": "px" },
        "blur": { "value": 0, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0, "androidDp": 0 } }
    },
    "e1": {
      "$description": "A resting card.",
      "$value": {
        "color": "{palette.slate-900}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 2, "unit": "px" },
        "blur": { "value": 12, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.06, "androidDp": 1 } }
    },
    "e2": {
      "$description": "A sticky header.",
      "$value": {
        "color": "{palette.slate-900}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 4, "unit": "px" },
        "blur": { "value": 24, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.1, "androidDp": 3 } }
    },
    "e3": {
      "$description": "A sheet, a FAB, or the floating tab bar.",
      "$value": {
        "color": "{palette.slate-900}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 8, "unit": "px" },
        "blur": { "value": 40, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.16, "androidDp": 6 } }
    },
    "pin": {
      "$description": "A map pin at rest, lifted off the tiles. Declares no `androidDp` on purpose: a pin is a small overlay on a map, not a Material surface, so Android draws the same shadow geometry as iOS rather than a dp step.",
      "$value": {
        "color": "{palette.black}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 1, "unit": "px" },
        "blur": { "value": 4, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.35 } }
    },
    "pinActive": {
      "$description": "The selected map pin — a little more lift so it reads above its neighbours.",
      "$value": {
        "color": "{palette.black}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 2, "unit": "px" },
        "blur": { "value": 6, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.45 } }
    }
  },
  "glassShadow": {
    "$type": "shadow",
    "$description": "The cast shadow under floating glass — deliberately NOT an `elevation` level, because glass is a separate elevation channel whose primary depth cue is the refracted backdrop (ADR-035, docs/09 §Glass legibility rule 7). Two stops: a tight contact shadow plus a soft ambient one. Light mode only; on dark a drop shadow is haze, not depth. Web only — native glass lifts via its container's `e3`.",
    "contact": {
      "$value": {
        "color": "{palette.slate-900}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 1, "unit": "px" },
        "blur": { "value": 3, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.1 } }
    },
    "ambient": {
      "$value": {
        "color": "{palette.slate-900}",
        "offsetX": { "value": 0, "unit": "px" },
        "offsetY": { "value": 8, "unit": "px" },
        "blur": { "value": 22, "unit": "px" },
        "spread": { "value": 0, "unit": "px" }
      },
      "$extensions": { "com.nextbus": { "opacity": 0.13 } }
    }
  },
  "glassRim": {
    "$type": "shadow",
    "$description": "The rim light on a glass pane: a thin, top-weighted inner highlight, because glass is lit from above — not a uniform ring, which reads as a heavy border. Per-mode alphas encode the ADR-035 contrast budget: the white top highlight quietens on dark (a white edge over-reads against a dark surface) while the bottom inset shadow *strengthens*, because the dark-mode tint has already lightened the body for it to work against. Inset, web only.",
    "top": {
      "light": {
        "$value": {
          "color": "{palette.white}",
          "offsetX": { "value": 0, "unit": "px" },
          "offsetY": { "value": 1, "unit": "px" },
          "blur": { "value": 0.5, "unit": "px" },
          "spread": { "value": 0, "unit": "px" },
          "inset": true
        },
        "$extensions": { "com.nextbus": { "opacity": 0.42 } }
      },
      "dark": {
        "$value": {
          "color": "{palette.white}",
          "offsetX": { "value": 0, "unit": "px" },
          "offsetY": { "value": 1, "unit": "px" },
          "blur": { "value": 0.5, "unit": "px" },
          "spread": { "value": 0, "unit": "px" },
          "inset": true
        },
        "$extensions": { "com.nextbus": { "opacity": 0.12 } }
      }
    },
    "bottom": {
      "light": {
        "$value": {
          "color": "{palette.black}",
          "offsetX": { "value": 0, "unit": "px" },
          "offsetY": { "value": -1, "unit": "px" },
          "blur": { "value": 1, "unit": "px" },
          "spread": { "value": 0, "unit": "px" },
          "inset": true
        },
        "$extensions": { "com.nextbus": { "opacity": 0.06 } }
      },
      "dark": {
        "$value": {
          "color": "{palette.black}",
          "offsetX": { "value": 0, "unit": "px" },
          "offsetY": { "value": -1, "unit": "px" },
          "blur": { "value": 1, "unit": "px" },
          "spread": { "value": 0, "unit": "px" },
          "inset": true
        },
        "$extensions": { "com.nextbus": { "opacity": 0.16 } }
      }
    }
  }
}
